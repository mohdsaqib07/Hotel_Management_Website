/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: ['lh3.googleusercontent.com','source.unsplash.com','plus.unsplash.com',"images.unsplash.com",'avatars.githubusercontent.com']
	}
};

export default nextConfig;
